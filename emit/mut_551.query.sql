SELECT 'Check Shredding Consistency' AS `task`;
SELECT 'Check Array Element Fallback Consistency' AS `task`;
SELECT '--- Shredded Data ---' AS `label`;
SELECT '--- No-Shredded Data ---' AS `label`;
SELECT '--- Full Variant Comparison ---' AS `label`;
SELECT 'Check Shredding Consistency' AS `task` GROUP BY 'Check Shredding Consistency';
SELECT max('Check Shredding Consistency') OVER () AS `task`;
SELECT 'Check Shredding Consistency' AS `task` GROUP BY 'Check Shredding Consistency';
SELECT DISTINCT 'Check Shredding Consistency' AS `task`;
SELECT 'Check Shredding Consistency' AS `task` ORDER BY 'Check Shredding Consistency' DESC;
SELECT 'Check Shredding Consistency' AS `task` GROUP BY 'Check Shredding Consistency';
SELECT DISTINCT 'Check Shredding Consistency' AS `task`;
SELECT 'Check Shredding Consistency' AS `task` GROUP BY 'Check Shredding Consistency';
SELECT 'Check Shredding Consistency' AS `task` LIMIT 100;
SELECT count('Check Shredding Consistency') OVER () AS `task`;
SELECT 'Check Shredding Consistency' AS `task` LIMIT 7, 2;
SELECT CAST((CAST('Check Shredding Consistency' AS CHAR(1))) AS DATE) AS `task`;
SELECT 'Check Shredding Consistency' AS `task` WHERE '--- Full Variant Comparison ---';
SELECT 'Check Shredding Consistency' AS `task` WHERE 'Check Array Element Fallback Consistency';
SELECT DISTINCT 'Check Shredding Consistency' AS `task`;
SELECT 'Check Array Element Fallback Consistency' AS `task` GROUP BY 'Check Array Element Fallback Consistency';
SELECT DISTINCT 'Check Array Element Fallback Consistency' AS `task`;
SELECT 'Check Array Element Fallback Consistency' AS `task` ORDER BY 'Check Array Element Fallback Consistency' ASC;
SELECT 'Check Array Element Fallback Consistency' AS `task` ORDER BY 'Check Array Element Fallback Consistency' ASC;
SELECT min('Check Array Element Fallback Consistency') OVER () AS `task`;
SELECT 'Check Array Element Fallback Consistency' AS `task` LIMIT 2;
SELECT 'Check Array Element Fallback Consistency' AS `task` ORDER BY 'Check Array Element Fallback Consistency' DESC;
SELECT 'Check Array Element Fallback Consistency' AS `task` WHERE 'Check Array Element Fallback Consistency';
SELECT '--- Shredded Data ---' AS `label` GROUP BY '--- Shredded Data ---';
SELECT count('--- Shredded Data ---') OVER () AS `label`;
SELECT '--- Shredded Data ---' AS `label` ORDER BY '--- Shredded Data ---' ASC;
SELECT CAST('--- Shredded Data ---' AS JSON) AS `label`;
SELECT '--- Shredded Data ---' AS `label` ORDER BY '--- Shredded Data ---' ASC;
SELECT '--- Shredded Data ---' AS `label` GROUP BY '--- Shredded Data ---';
SELECT json_length(CAST('--- Shredded Data ---' AS JSON)) AS `label`;
SELECT '--- Shredded Data ---' AS `label` ORDER BY '--- Shredded Data ---' DESC;
SELECT DISTINCT '--- Shredded Data ---' AS `label`;
SELECT DISTINCT '--- Shredded Data ---' AS `label`;
SELECT '--- No-Shredded Data ---' AS `label` WHERE '--- Shredded Data ---';
SELECT '--- No-Shredded Data ---' AS `label` ORDER BY '--- No-Shredded Data ---' DESC;
SELECT DISTINCT '--- No-Shredded Data ---' AS `label`;
SELECT '--- No-Shredded Data ---' AS `label` WHERE 'Check Shredding Consistency';
SELECT '--- No-Shredded Data ---' AS `label` GROUP BY '--- No-Shredded Data ---';
SELECT max('--- No-Shredded Data ---') OVER () AS `label`;
SELECT DISTINCT '--- No-Shredded Data ---' AS `label`;
SELECT '--- No-Shredded Data ---' AS `label` GROUP BY '--- No-Shredded Data ---';
SELECT DISTINCT '--- No-Shredded Data ---' AS `label`;
SELECT '--- No-Shredded Data ---' AS `label` GROUP BY '--- No-Shredded Data ---';
SELECT '--- Full Variant Comparison ---' AS `label` GROUP BY '--- Full Variant Comparison ---';
SELECT count('--- Full Variant Comparison ---') OVER () AS `label`;
SELECT '--- Full Variant Comparison ---' AS `label` ORDER BY '--- Full Variant Comparison ---' ASC;
SELECT '--- Full Variant Comparison ---' AS `label` LIMIT 7, 0;